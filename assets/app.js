const canvas=document.getElementById('starfield');const ctx=canvas.getContext('2d');let stars=[];function resize(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;ctx.scale(devicePixelRatio,devicePixelRatio);stars=Array.from({length:Math.min(150,Math.floor(innerWidth/10))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.25+.2,a:Math.random()*.55+.15,v:Math.random()*.12+.025}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const s of stars){s.y-=s.v;if(s.y<0)s.y=innerHeight;ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fillStyle=`rgba(185,222,255,${s.a})`;ctx.fill()}requestAnimationFrame(draw)}resize();draw();addEventListener('resize',resize);const watcher=new IntersectionObserver(items=>items.forEach(x=>{if(x.isIntersecting){x.target.classList.add('visible');watcher.unobserve(x.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>watcher.observe(x));function makeProjectCardClickable(id,url,label){const card=document.getElementById(id);if(!card)return;card.tabIndex=0;card.setAttribute('role','link');card.setAttribute('aria-label',label);card.style.cursor='pointer';card.addEventListener('click',event=>{if(!event.target.closest('a,button'))window.location.href=url});card.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();window.location.href=url}})}makeProjectCardClickable('commerce','commerce-case.html','打开跨境电商内容自动化案例');makeProjectCardClickable('agents','agents-case.html?v=20260808','打开智能体与工作流研究案例');
